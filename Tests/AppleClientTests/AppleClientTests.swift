import XCTest
@testable import AppleClient

final class AppleClientTests: XCTestCase {
    func testModuleLoads() {
        // Smoke test — AppleClient compiles and links against Core.
        XCTAssertTrue(true)
    }
}
