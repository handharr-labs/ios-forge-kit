import XCTest
@testable import ForgeUI

final class ForgeUITests: XCTestCase {
    func testModuleLoads() throws {
        // Smoke test — ForgeUI compiles and links.
        XCTAssertTrue(true)
    }
}
